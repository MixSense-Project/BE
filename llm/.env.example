# Copy this file to .env and fill in values (do NOT commit .env to git)
OPENAI_API_KEY="YOUR_OPENAI_API_KEY_HERE"

# Optional: choose model for intent parsing
MIXSENSE_OPENAI_MODEL="gpt-4o-mini"

# Optional: allow LLM to control ranking_weights (NOT recommended for v1)
# MIXSENSE_LLM_WEIGHTS=1

# Optional: enable YouTube search for '이 노래 뭐야?' style queries
YOUTUBE_API_KEY=
